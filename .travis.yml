language: generic
sudo: required
services:
  - docker
before_install:
  - docker build -t vladasha256/docker-react -f Dockerfile.dev .
script:
  - docker run -e CI=true vladasha256/docker-react npm run test -- --coverage
